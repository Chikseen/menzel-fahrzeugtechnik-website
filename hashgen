
    try {
        const hashpass = await bcrypt.hash(data.passwort, 10)
        temp = hashpass;
        console.log("hashed: ");
        console.log(hashpass);
    }
    catch {
        response.status(500).send();
    }
    console.log("1");
    